app:
  environment: cer

spring:
  # Certification environment with moderate pool size
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 2

logging:
  level:
    root: WARN
    com.eatsy.monolith: INFO
    com.zaxxer.hikari: INFO
